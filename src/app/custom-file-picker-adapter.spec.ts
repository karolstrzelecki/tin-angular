import { CustomFilePickerAdapter } from './custom-file-picker-adapter';

describe('CustomFilePickerAdapter', () => {
  it('should create an instance', () => {
    expect(new CustomFilePickerAdapter()).toBeTruthy();
  });
});
