import { RegistrationFormValues } from './registration-form-values';

describe('RegistrationFormValues', () => {
  it('should create an instance', () => {
    expect(new RegistrationFormValues()).toBeTruthy();
  });
});
